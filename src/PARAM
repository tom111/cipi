# Init File for Information Complexity Program				
# Comments start with #							




# Definitions:								
#									
# N - Number of Nodes ( > 24 will result in inconsitent behaviour)	
# SetIterations - Number of iterativ Scalings 				
N = 3 
SetIterations = 50000


#input format: sequence of characters,line beginning with ">" seperates samples; use sliding window for read input
#InputType=CharacterSequence
#
#samples coded by integers
#InputType=Integer
#
#list of samples, every line is one sample
InputType=Sample
#p empirical file, line consists of konfiguration tab probability
#InputType=Empirical

#name prefix for the output file containing the distributions, leave empty for no output
#OutputFilePrefix = 
OutputFilePrefix = A

#alphabet
Alphabet = AGCT

#hypergraph, seperate hypergraphs with ";"(";" behind last hypergraph is not necessary)
#Ax or ax for hypergraph with all sets with x elements
#Hypergraph = A2;a1
#Hypergraph = (1,2);(1),(2);
Hypergraph =

#max number of threads running at the same time(including main thread)
NumberOfProcesses = 3

## Shall we print the result only and be silent otherwise ??
## If this is set to true then the output will be I1 I2 I3, separated by ResultSep
## Note that this only works if "Hypergraph" is empty, i.e. we are working with the 
## uniform interactions.
ResultOnly=true
ResultSep=,